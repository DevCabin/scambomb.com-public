# ScamBomb Jeff Training HTML

Open `index.html` in a browser to view the training mini-site.

Files included:
- index.html
- one HTML page per training guide
- style.css

This is a simple static HTML package. It can be used locally, uploaded to a static host, or incorporated into a larger bootcamp project by CLINE.
